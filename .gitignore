# Secrets — only .env.example files (placeholders) are committed
.env
.env.*
!.env.example

__pycache__/
*.pyc
.venv/
node_modules/
dist/
backend/staticfiles/

# One-off proof/deliverable screenshots (e.g. for assignments) — not site source
/screenshots/
