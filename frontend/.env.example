# Base URL of the Django API: include /api, no trailing slash.
# Local dev: http://localhost:8000/api
# Production: https://<your-backend>.onrender.com/api
# Vite bakes this into the bundle at BUILD time, so rebuild after changing it.
VITE_API_URL=http://localhost:8000/api
